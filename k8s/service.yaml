apiVersion: v1
kind: Service
metadata:
  name: todo-api-service
  namespace: todo-api
  labels:
    app: nestjs-todo-api
spec:
  type: ClusterIP
  selector:
    app: nestjs-todo-api
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
  sessionAffinity: None
